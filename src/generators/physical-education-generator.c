#include <stdio.h>
#include <stdlib.h>
#include "physical-education-generator.h"

char *
generate_physical_education_objective
(int level, int grade, int domain_level, char *topic)
{
	char * objective = (char *) malloc(512);
	return objective;
}

