#include <stdlib.h>
#include <string.h>
#include "english-generator.h"

char *
generate_english_objective
(int level, int grade, int domain_level, char *topic)
{
    char * objective = (char *) malloc(512);
    return objective;
}