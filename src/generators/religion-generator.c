#include <stdio.h>
#include <stdlib.h>
#include "religion-generator.h"

char *
generate_religion_objective
(int level, int grade, int domain_level, char *topic)
{
	char * objective = (char *) malloc(512);
	return objective;
}

