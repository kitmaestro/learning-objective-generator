#include <stdlib.h>
#include <string.h>
#include "spanish-generator.h"

char * 
generate_spanish_objective
(int level, int grade, int domain_level, char* topic)
{
    char * objective = (char *) malloc(512);
    return objective;
}